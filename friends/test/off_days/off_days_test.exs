defmodule OffDaysTest do
  use ExUnit.Case
  alias Friends.OffDays.OffDayManager

  test "it updates all the off days for a given person" do

      new_off_days = OffDayManager.set_off_days()

      # TODO: Create the test case
    end
end
